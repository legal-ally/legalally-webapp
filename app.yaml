runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  STORAGE_DIR: /tmp
  APP_NAME: Legal-Ally
  APP_KEY: 4tRCVlCe6Z9p1LrVa1GUPqVNC1iRstFvZIVIXddj3cU=
  APP_DEBUG: true
  APP_LOG_LEVEL: debug